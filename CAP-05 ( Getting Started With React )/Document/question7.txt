How do you add react to a web application?
First we have to add React packages and reactDOM packages in your html and then we have to declare a a root div element in the html then
we will access it in the script tag and then we will create a ractDOM then render it with the root element . 
